- .to_file() -- combined stdout and stderr and pipe to file.
- .to_handle() -- combined stdout and sterr and pipe to file handle.
- Refactor change directory to use a context manager.
- .as_bash_command() - prepend command with bash -c ? What does 'shell' do? Bash environment vars?
